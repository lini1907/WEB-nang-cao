import { Component,OnInit } from '@angular/core'; import { CustomerService,CustomerGroup } from '../customer.service';
@Component({selector:'app-customer-groups',templateUrl:'./customer-groups.html',styleUrl:'./customer-groups.css',standalone:false}) export class CustomerGroupsComponent implements OnInit {groups:CustomerGroup[]=[]; loading=true; error=''; constructor(private service:CustomerService){} ngOnInit(){this.service.getCustomerGroups().subscribe({next:data=>{this.groups=data;this.loading=false},error:()=>{this.error='Không thể tải danh sách khách hàng.';this.loading=false}});} trackGroup(_:number,g:CustomerGroup){return g.CustomerTypeId;} }
